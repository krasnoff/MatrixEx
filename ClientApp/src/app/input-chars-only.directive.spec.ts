import { InputCharsOnlyDirective } from './input-chars-only.directive';

describe('InputCharsOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new InputCharsOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
